import React from "react";

const Button = () => {
    return(<div>

    <button onClick={() => alert("¡Botón presionado!")}>
                Presionar
            </button>
    </div>);
};

export { Button };